
{
  "name": "social-media-parser",
  "version": "1.0.0",
  "private": true,
  "description": "An application to parse and display social media messages based on keywords.",
  "scripts": {
    "dev": "echo 'No dev script needed for this no-build setup. Just serve index.html.' && exit 0",
    "build": "mkdir public && cp index.html index.tsx App.tsx types.ts constants.ts public/ && cp -r components services public/"
  },
  "dependencies": {
    "@google/genai": "^1.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.20",
    "@types/react": "^18.2.58",
    "@types/react-dom": "^18.2.19",
    "@vercel/node": "^3.0.25",
    "typescript": "^5.3.3"
  }
}