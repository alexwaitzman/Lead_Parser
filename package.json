
{
  "name": "social-media-parser",
  "version": "1.0.0",
  "private": true,
  "description": "An application to parse and display social media messages based on keywords.",
  "scripts": {
    "dev": "echo 'No dev script needed for this no-build setup. Just serve index.html.' && exit 0",
    "build": "echo 'No build script needed for frontend. Vercel will build the api directory.' && exit 0"
  },
  "dependencies": {
    "@google/genai": "^1.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.20",
    "@types/react": "^18.2.58",
    "@types/react-dom": "^18.2.19",
    "@vercel/node": "^3.0.25",
    "typescript": "^5.3.3"
  }
}
